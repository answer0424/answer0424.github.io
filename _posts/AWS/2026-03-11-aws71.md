---
layout: post
title: SSH 클라이언트를 사용하여 Linux 인스턴스 연결
date: 2026-03-11 09:00:00 +09:00
categories: [AWS, EC2, Instance, SSH]
tags: [aws]
image:
    path: /assets/img/aws/AWS.png
---

## 핵심 요약

- Linux EC2 인스턴스는 SSH 프로토콜을 사용해 접속
- 접속을 위해 필요한 핵심요소
  - EC2 Key Pair(.pem)
  - 퍼블릭 IP 또는 퍼블릭 DNS
  - SSH 클라이언트
  - AMI에 맞는 사용자 계정
- 기본 SSH 접속 명령 예:

```bash
ssh -i my-key.pem ec2-user@<EC2-Public-IP>
```

---

## 1. SSH 연결 사전 조건

> SSH로 EC2 Linux 인스턴스에 연결하기 전에 다음 조건이 충족되어야 함

#### 인스턴스 상태

- 인스턴스가 running 상태
- 상태 검사 통과 필요

<br>

#### 보안 그룹 설정

> EC2 보안그룹에서 SSH 인바운드 규칙이 허용되어야 함

| 항목 | 값 |
|-|-|
| Type | SSH |
| Port | 22 |
| Source | 내 IP 또는 허용 네트워크 |

<br>

#### 프라이빗 키 준비

> EC2 인스턴스 생성 시 다운로드 한 Key Pair(.pem) 필요

```bash
chmod 400 my-key.pem
```

<br>


#### SSH 클라이언트 설치